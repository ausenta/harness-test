---
id: WI-000
title: Health endpoint returning app version
status: in-progress
branch: agent/WI-000
worktree: ../wt-WI-000
db: postgresql://postgres:agent@localhost:57384/app_WI_000
issue: 1
pr: 2
spec-refs: [DRY-RUN]
db-migration: false
dast: false
risk-tier: trivial
break-glass: false
grilled:
slop-grade:
board-chair-verdict:
approved-by:
approved-at:
depends-on: []
estimate: 0.25d
---
# WI-000 — Health endpoint returning app version
## Objective
GET /health returns 200 with JSON {"status":"ok","version":<value from config>}. Exists purely to prove the pipeline end to end.
## In Scope
- One route, one config read, tests
## OUT OF SCOPE (hard fence)
- Auth, logging, metrics, any other endpoint
## Seams Under Test
- `GET /health` HTTP contract
## File Surface (only files the agent may touch)
- src/routes/health.ts
- src/config.ts (read only — add nothing)
- tests/health.test.ts (additions only; locked skeleton immutable)
## Acceptance Criteria (each must map to a spec-ref)
1. [ ] GET /health returns HTTP 200 (DRY-RUN)
2. [ ] Body is {"status":"ok","version":X} where X equals config version, not a literal (DRY-RUN)
## Test Plan (executed verbatim by agent, re-run by orchestrator)
1. curl -s localhost:3000/health → 200, JSON matches config version
## Security Requirements
- [ ] No secrets in code/config; env vars only
- [ ] Gates green: audit, gitleaks, semgrep (evidence below)
## New Dependencies
- none
---
## Evidence (agent fills — raw gate output)
## Implementation Notes (agent fills)
## Questions for Orchestrator (agent fills if blocked)
## Rejection (orchestrator fills if rejected)
