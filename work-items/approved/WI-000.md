---
id: WI-000
title: Health endpoint returning app version
status: approved
stale: resolved   # was stale; rebased onto main + clean re-verify before squash merge (b4b574c)
branch: agent/WI-000 (merged, deleted)
worktree: ../wt-WI-000 (torn down)
db: postgresql://postgres:agent@localhost:57384/app_WI_000 (torn down)
issue: 1
pr: 2
spec-refs: [DRY-RUN]
db-migration: false
dast: false
risk-tier: trivial
break-glass: false
grilled: true
slop-grade: A
board-chair-verdict: concur-with-conditions
approved-by: human (byron.bricker)
approved-at: 2026-07-20T16:44Z
depends-on: []
estimate: 0.25d
---
# WI-000 — Health endpoint returning app version
## Objective
GET /health returns 200 with JSON {"status":"ok","version":<value from config>}. Exists purely to prove the pipeline end to end.
## In Scope
- One route, one config read, tests
## OUT OF SCOPE (hard fence)
- Auth, logging, metrics, any other endpoint
## Seams Under Test
- `GET /health` HTTP contract
## File Surface (only files the agent may touch)
- src/routes/health.ts
- src/config.ts (read only — add nothing)
- tests/health.test.ts (additions only; locked skeleton immutable)
## Acceptance Criteria (each must map to a spec-ref)
1. [x] GET /health returns HTTP 200 (DRY-RUN)
2. [x] Body is {"status":"ok","version":X} where X equals config version, not a literal (DRY-RUN)
## Test Plan (executed verbatim by agent, re-run by orchestrator)
1. curl -s localhost:3000/health → 200, JSON matches config version
## Security Requirements
- [x] No secrets in code/config; env vars only
- [x] Gates green: audit, gitleaks, semgrep (evidence below)
## New Dependencies
- none
---
## Evidence (agent fills — raw gate output)
`scripts/gates.sh` is now **GREEN tree-wide** (exit 0). The prior semgrep blocker was a harness-level issue in the CI-YAML files; it was fixed on `origin/main` (commit `48f9983`) and the two files were synced read-only into this worktree (no merge/rebase). Fresh full run:

```
=== GATE: Lint ===
> eslint .
=== GATE: Unit tests + coverage ===
> vitest run --coverage
 Test Files  1 passed (1)
      Tests  2 passed (2)
 % Coverage report from v8
Statements   : 100% ( 7/7 )
Branches     : 100% ( 2/2 )
Functions    : 100% ( 2/2 )
Lines        : 100% ( 7/7 )
=== GATE: Dependency audit (high/critical) ===
found 0 vulnerabilities
=== GATE: Secrets scan (full tree) ===
7 commits scanned.
scanned ~262256 bytes (262.26 KB) in 75.2ms
no leaks found
=== GATE: SAST (semgrep, curated ruleset) ===
(no findings)
=== GATE: Filesystem vulns + IaC misconfig (trivy) ===
package-lock.json  npm  0 vulnerabilities
Misconfigurations: 0
=== GATE: IaC policy scan (checkov — runs only if IaC present) ===
no IaC files — skipped
=== GATE: DAST baseline (only if item flagged dast: true) ===
not a DAST-flagged item — skipped

ALL GATES GREEN
=== EXIT CODE: 0 ===
```

- **Lint (eslint):** pass, no findings.
- **Unit tests + coverage (vitest):** `Test Files 1 passed (1) / Tests 2 passed (2)`; coverage 100% Statements (7/7), Branches (2/2), Functions (2/2), Lines (7/7).
- **Test Plan (live curl):** `GET localhost:3000/health` → `200`, body `{"status":"ok","version":"0.1.0"}`, matches `config.version` (env `APP_VERSION` unset → default `0.1.0`). AC1 ✔ AC2 ✔ (version read from config, not a literal).
- **Dependency audit:** `found 0 vulnerabilities`.
- **Secrets (gitleaks):** `no leaks found` (7 commits, ~262 KB).
- **Trivy (fs + IaC):** package-lock.json npm 0 vulns; 0 misconfigurations.
- **checkov:** no IaC files — skipped. **DAST:** not flagged — skipped.
- **SAST (semgrep):** ✔ no findings — the previously-blocking CI-YAML findings are resolved by the main-side fix synced read-only.

## Implementation Notes (agent fills)
- Sole deliverable: created `src/routes/health.ts` exporting `healthRouter` (the import `src/app.ts:5` already expected). One route, one config read; no other files touched in the File Surface.
- Version comes from `config.version` (`src/config.ts` → `process.env.APP_VERSION ?? '0.1.0'`), never a literal, per AC2.
- Queue bookkeeping only: synced canonical item from `origin/main` into `in-progress/`, removed stale backlog copy (commit `35b6e32`). No merge/rebase of main.

## Questions for Orchestrator (agent fills if blocked)
**RESOLVED — fixed on main (option 1).** The prior BLOCKER (semgrep firing on out-of-surface CI-YAML: `github-actions-mutable-action-tag`, `gha-curl-pipe-shell`) was a harness-level issue. The orchestrator applied the security-correct fix on `origin/main` (commit `48f9983`); the two files (`.github-workflows-gates.yml`, `.github/workflows/gates.yml`) were synced read-only into this worktree via `git show` — no merge/rebase of main. `scripts/gates.sh` now runs green tree-wide (evidence above). No out-of-surface source edits were made in `agent/WI-000`.
## Review Findings (orchestrator — post judge-pass)
- Surviving findings: **0**.
- Dropped in judge pass: **1** — spec-compliance (warning) flagged `.github/workflows/gates.yml` + `.github-workflows-gates.yml` as out-of-surface. Dropped: byte-identical to current `main`, zero net change on merge, agent escalated the blocker correctly (Questions) and synced main's fix read-only. Stale-merge-base artifact, not a scope violation.

## Board Packet (orchestrator fills at docketing)
- **Recommendation:** APPROVE (docketed for board). Merging only after human sign-off in `/board`.
- **Surviving findings:** 0. **Dropped:** 1 (workflow scope warning — stale-base artifact; see Review Findings).
- **Criteria coverage:** 2/2, each with a locked-test citation:
  - AC1 → `tests/health.test.ts::AC1: GET /health returns HTTP 200`
  - AC2 → `::AC2: body … version equals config version` (asserts `config.version`, not a literal)
- **Evidence integrity:** independent `verify-item.sh` gate re-run == agent Evidence block, ALL GATES GREEN (lint · 2/2 tests · 100% cov · 0 audit · no leaks · semgrep clean · trivy 0/0).
- **Diffstat (net vs current `main`):** `src/routes/health.ts +10`, `tests/health.test.ts +18`, `.locked-tests +1`, plus the work-item move. No source outside the file surface.
- **STOPAISLOP grade:** **A** (10-line route, one config read, no bloat; slop-reviewer not spawned at trivial tier — grade by orchestrator read).
- **⚠️ STALE — merge-safety condition:** branch is behind `main` (`48f9983` gates hardening, `21e4d76` WI-H1/WI-H2). Board MUST: (1) rebase `agent/WI-000` onto `main`, (2) re-run `verify-item.sh` and merge only on clean re-verify, (3) `gh pr merge --squash` (3-way) — NEVER a raw two-dot diff (would delete WI-H1/WI-H2 and revert the hardened workflow). Post-rebase net diff should be `health.ts` + tests + `.locked-tests` + item move only.
- **Proposed grill questions:**
  1. Which requirement closes? → DRY-RUN pipeline proof (no product spec).
  2. Rollback path? → single revert; no DB migration.
  3. Confirm the stale-branch **rebase + re-verify** ran clean and that WI-H1/WI-H2 survive the merge.

## Board Record (2026-07-20)
- **Chair verdict:** CONCUR WITH CONDITIONS (independent verification — confirmed workflow files byte-identical to main via `git diff`/`merge-tree`; recommendation survived scrutiny).
- **Human ruling:** APPROVE (byron.bricker), conditional on a shown clean post-rebase re-verify.
- **Conditions — all met:** (1) rebased `agent/WI-000` onto main, independent `gates.sh` re-run GREEN, net diff = deliverable only; (2) queue duplication reconciled — rebase dropped the branch's item-file copies, so only main's canonical copy remains; (3) LESSONS + agent-checklist updated re: agents committing to their own gate files.
- **Grill rulings:** (a) merge mechanic confirmed rebase→re-verify→squash; (b) policy — agents sync out-of-surface fixes read-only but do NOT commit them (see [[LESSONS]] + ADR-0001); (c) rollback correction accepted — reverting WI-000 requires also reverting the scaffold's `src/app.ts` wiring or main goes red (risk accepted for dry run, ADR-0001); (d) queue reconciled as above; (e) `/health` contract locked as shipped.
- **Merge:** squash `b4b574c` (PR #2), Issue #1 auto-closed, branch + worktree + DB torn down.

## Rejection (orchestrator fills if rejected)
